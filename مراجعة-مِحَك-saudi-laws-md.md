# مراجعة مِحَك — saudi-laws-md

**التاريخ:** 2026-08-18 · **الالتزام المُراجَع:** `d29e05d` · **النوع المكتشف:** تطبيق (أداة استخراج + مُدوَّنة بيانات) — لا لعبة، فالرابرك المطبَّق هو رابرك التطبيقات.

**المنهجية:** قراءة كاملة لكل ملفات `scripts/` و`tests/` و`.github/`، تشغيل فعلي للاختبارات (150 نجحت) و`ruff` (نظيف) و`lint_corpus` على 2737 ملفًا، تحليل إحصائي للمُدوَّنة كاملة، وتجارب معملية للتحقق من كل فرضية قبل تسجيلها. الملاحظات المُعلَّمة بـ **(مُتحقَّق منه معمليًا)** أُعيد إنتاجها فعليًا، لا استُنتجت من قراءة الكود.

> توجد مراجعة سابقة في `ENGINEERING_REVIEW.md` بتاريخ 2026-07-22. هذه مراجعة مستقلة على الحالة الراهنة؛ ملاحظاتها M‑1…M‑5 كلها مُعالَجة فعلًا في الكود الحالي (تحقّقت من ذلك)، وما يلي ملاحظات جديدة على ما بعدها.

---

## 1. نظرة عامة سريعة

المشروع أداة Python تسحب نصوص الأنظمة واللوائح السعودية من موقعين خارجيين (`qanoonsa.com` و`nezams.com`)، وتحوّلها إلى Markdown موحّد بـ front matter، وتخزّنها في `laws/` مصنّفة بمجلدات، مع فهرس آلي `index.json` لأدوات البحث وRAG. الأداة والمُدوَّنة في نفس المستودع، والتحديث الدوري يفتح Pull Request مسودة للمراجعة اليدوية.

**التقنيات:** Python ≥3.10 · `requests ≥2.31` · `beautifulsoup4 ≥4.12` + `lxml ≥4.9` · `pytest ≥7` · `ruff ≥0.6` · GitHub Actions (3 workflows) + Dependabot.

**الحجم:**

| العنصر | العدد |
|---|---|
| كود المصدر (`scripts/`) | 22 ملفًا · ‏2,511 سطرًا |
| الاختبارات (`tests/`) | 16 ملفًا · ‏1,813 سطرًا + 3 fixtures HTML |
| المُدوَّنة (`laws/`) | 2,737 وثيقة Markdown · 27 مجلد تصنيف · ~43 ميغابايت |
| الفهرس | `index.json` ‏(2,737 مدخلًا · 1.4 ميغابايت) |

المراجعة غطّت **100% من الكود** و**تحليلًا إحصائيًا شاملًا على 100% من المُدوَّنة**، مع فحص يدوي لعيّنة من الوثائق المشبوهة.

---

## 2. المراجعة التقنية والهندسية (45%)

### 2.1 البنية المعمارية وفصل المسؤوليات — **9/10**

الفصل حقيقي لا شكلي: `fetch` (شبكة) / `discover` (خرائط المواقع) / `adapters` (تحويل HTML) / `schema` (نموذج موحّد) / `classify` (تصنيف) / `formatter` (إخراج) / `report` (تقارير). كل طبقة تعتمد على التي تحتها فقط، ولا يوجد ملف "يسوّي كل شيء".

أفضل قرار معماري في المستودع هو سجل المصادر: `adapters/base.py` يجعل كل adapter يعلن بياناته الوصفية كصفات صنف، و`adapters/__init__.py` يشتق منها كل الجداول:

```python
ADAPTERS: tuple[type[BaseAdapter], ...] = (QanoonsaAdapter, NezamsAdapter)
_BY_SOURCE = {a.source: a for a in ADAPTERS}
```

و`discover.py` يشتق منها بدوره:

```python
SITE_INDEX = {a.source: a.sitemap_index for a in ADAPTERS if a.sitemap_index}
_ALLOWED_HOSTS = {h for a in ADAPTERS for h in a.hosts}
```

إضافة مصدر ثالث فعلًا = صنف واحد + سطر واحد. هذا يحلّ ملاحظة "تركّز معرفة المصادر في 4 مواضع" من المراجعة السابقة حلًّا صحيحًا.

**الخصم الوحيد:** `run()` في `scripts/main.py` تجاوزت 130 سطرًا وتخلط ثلاث مسؤوليات: تعريف الوسائط، وحلّ مصادر الروابط (`--discover`/`--from-file`/مباشرة)، وحلقة المعالجة والإحصاء. الحل: استخراج `_build_parser()` و`_collect_urls(args, fetcher) -> list[str]` و`_process_urls(...) -> RunStats`، فتصبح `run()` عشرين سطرًا وتصبح كل قطعة قابلة للاختبار وحدها.

### 2.2 قابلية القراءة والصيانة — **8/10**

التعليقات في هذا المستودع من أفضل ما رأيت في مشاريع الاستخراج: تشرح **"ليش"** لا "وش"، وكثير منها يوثّق قرارًا هندسيًا وسببه التاريخي. مثال في `formatter.py` — `disambiguated_filename()`:

> «sanitize_filename يقتطع من نهاية الاسم، فحين يبلغ الجذع وحده حدَّ الطول تضيع لاحقة التمييز كلها ويعود الاسم مطابقًا للاسم المتصادم — وهو ما كان يُدخل حلقة `_resolve_collision` في دوران بلا نهاية.»

هذا تعليق يمنع مطوّرًا لاحقًا من "تبسيط" الدالة وإعادة إدخال العطل. نفس المستوى في `urls.py` و`main.build_source_index()` و`schema._NOISE_PATTERNS`.

التسمية واضحة، والدوال قصيرة عمومًا، والتداخل ضحل. المشكلة الوحيدة هي طول `run()` أعلاه وحلقة `QanoonsaAdapter.parse` (نحو 60 سطرًا بحالة داخلية من خمسة متغيّرات: `current`, `current_bab`, `current_fasl`, `clauses`, `intro`) — قابلة للفهم لكنها الموضع الأصعب في المستودع، ويستحق استخراج آلة الحالة إلى `_ArticleCollector` صغيرة.

### 2.3 التكرار (DRY) و Code Smells — **7/10**

ثلاثة تكرارات حقيقية:

**(أ) قارئ حقل القائمة مكرّر ثلاث مرات حرفيًا.** نفس التعبير النمطي ونفس الدالة في `build_index.py` و`audit_duplicates.py` و`canonicalize_urls.py`:

```python
_LIST_FIELD_RE = re.compile(r'^also_available_from:\s*\[(.*?)\]\s*$', re.MULTILINE)

def _extract_list(text: str) -> list[str]:
    ...
```

بينما `frontmatter.py` هو الموضع الطبيعي لها (فيه `set_list_field` بالفعل، بلا `read_list_field` مقابل). الحل: أضِف `read_list_field(text, field)` في `frontmatter.py` واحذف النسخ الثلاث.

**(ب) `canonicalize_urls.run()` تعيد تنفيذ منطق `canonicalize_file()` كاملًا** لمجرد دعم `--dry-run` — نفس الحلقة ونفس شرط `cu == own or cu in seen` مكتوبة مرتين في نفس الملف. الحل: اجعل `canonicalize_file(path, dry_run=False)` تُرجع ما سيتغيّر، ونادِها من `run()` مرة واحدة.

**(ج) قراءة `source_url` مكرّرة بين `main.py` و`frontmatter.py`.** هذه موثَّقة بسبب معقول (المسار الساخن)، لكن السبب لم يعد قائمًا: `_front_matter_head()` في `main.py` صارت تقرأ الكتلة فقط بدل الملف كله، فيمكن نقلها إلى `frontmatter.py` واستخدامها من الطرفين بلا كلفة.

**رائحة إضافية:** `lint_corpus.py` يستورد رمزين خاصّين من وحدة أخرى — `from .schema import _BROKEN_TITLE_RE, _NOISE_PATTERNS`. الشرطة السفلية تعني "خاص"، فاستيرادها عبر الوحدات يكسر العقد. اجعلهما عامّين (`BROKEN_TITLE_RE`, `NOISE_PATTERNS`) ما دام مقصودًا مشاركتهما.

### 2.4 معالجة الأخطاء والحالات الحدّية — **5/10**

أضعف نقطة هندسية في المشروع، وفيها عطلان مؤكَّدان.

#### 🔴 (أ) نافذة فقدان وثيقة: الحذف قبل الكتابة *(مُتحقَّق منه معمليًا)*

في `scripts/main.py` — `process_html()`، حين ينتقل ملف من مسار لآخر (تغيّر تصنيف أو عنوان):

```python
if existing is not None:
    old_entry = existing.get(doc.source_url)
    if old_entry is not None and old_entry.path != path and old_entry.path.exists():
        old_entry.path.unlink()                    # ← الحذف أولًا
        prune_empty_dirs(old_entry.path.parent, out_dir)
path.parent.mkdir(parents=True, exist_ok=True)
path.write_text(format_document(doc), encoding="utf-8")   # ← ثم الكتابة
```

الملف القديم يُحذف **قبل** أن تُكتب النسخة الجديدة. أي فشل بينهما (قرص ممتلئ، انقطاع العملية، صلاحيات) يفقد الوثيقة نهائيًا.

أعدت إنتاجه: وثيقة قائمة، ثم `write_text` ترفع `OSError(28)`، والنتيجة:

```
فشل الكتابة: [Errno 28] No space left on device
الملف القديم ما زال موجودًا؟ False
كل ملفات المخرجات: []
```

الوثيقة اختفت كليًا. وهذا يقع في `update-corpus.yml` الذي يشغّل `reclassify` بعد كل جلب — أي بالضبط حين تنتقل الملفات بين المجلدات.

**الحل:** اكتب أولًا ثم احذف، وبكتابة ذرّية:

```python
path.parent.mkdir(parents=True, exist_ok=True)
tmp = path.with_suffix(path.suffix + ".tmp")
tmp.write_text(format_document(doc), encoding="utf-8")
tmp.replace(path)                                  # ذرّية على نفس نظام الملفات
if old_entry is not None and old_entry.path != path and old_entry.path.exists():
    old_entry.path.unlink()
    prune_empty_dirs(old_entry.path.parent, out_dir)
```

نفس النمط ينطبق على `reclassify.reclassify()` (تكتب الجديد ثم `path.unlink()` — ترتيبها صحيح أصلًا، لكنها بلا كتابة ذرّية) وعلى كل `write_text` في سكربتات الصيانة.

#### 🔴 (ب) بوّابة الجودة تمرّر وثائق جوفاء — 191 ملفًا *(مُتحقَّق منه معمليًا)*

`lint_corpus.py` يرفض الوثيقة التي **لا متن لها إطلاقًا**:

```python
content = re.sub(r"^\s*#\s.*$", "", body, count=1, flags=re.MULTILINE).strip()
if not content:
    errors.append("وثيقة بلا متن: front matter وعنوان فقط")
```

لكن `if not content` يمرّ عليه أي متن مهما كان تافهًا. الحصيلة على المُدوَّنة الحالية:

| الحالة | العدد | الوصف |
|---|---:|---|
| متنها سطر الإصدار فقط | **191** | لا نصّ قانوني إطلاقًا |
| متنها ضجيج واجهة خالص `English صدر بموجب …` | **12** | صفر محتوى |
| متنها كلمة `تحميل` فقط | 2 | الصفحة الأصل مجرّد رابط تنزيل PDF |
| متنها `#N/A` | 1 | خطأ صيغة جدول بيانات في المصدر |
| متن أقل من 500 حرف | 570 (20.8%) | يشمل ما سبق |

مثال حقيقي — `laws/أمر ملكي/أمر ملكي رقم (آ ٥٦٧) إنشاء هيئة لتطوير الأحساء.md`، الملف كامل المتن:

```markdown
# أمر ملكي رقم (آ / ٥٦٧) إنشاء هيئة لتطوير الأحساء

صدر في: ٤ من شوال ١٤٤٣هـ الموافق: ٥ من مايو ٢٠٢٢م
```

أمر ملكي بإنشاء هيئة، والنصّ كله مفقود. ومع ذلك:

```
$ python -m scripts.lint_corpus laws ; echo exit=$?
فُحص 2737 ملفًا.
exit=0
```

الـ CI أخضر بينما **7% من المُدوَّنة وثائق جوفاء تدّعي أنها نصوص أنظمة**. في مشروع غرضه المعلن نصوص قانونية موثوقة، هذا أخطر ما في التقرير: القارئ يفتح ملف "أمر ملكي" فيجد تاريخًا، ويظن أن هذا كل ما في الأمر الملكي.

لهذا سببان في الكود:

1. **`prose_to_markdown` لا يطبّق تنظيف بادئات الـ CMS.** في `qanoonsa.py` تُزال البادئة في حلقة المواد (`_CMS_LABEL_PREFIX_RE.sub("", text)`) لكن مسار الوثيقة النثرية يستدعي `prose_to_markdown(content, skip=...)` مباشرة بلا تنظيف — فمرّت الـ 12 وثيقة `English صدر بموجب …`.
2. **`skip` لا تتضمّن سطر `صدر في:`.** السطر يُستخرج إلى `doc.issued_date` في حلقة المواد، لكن مجموعة `skip` للمسار النثري تحوي `{"English"}` + `gazette_ref` + سطور `صدر بموجب` فقط — فيُعاد إخراجه في المتن، فتنتج وثيقة "متنها سطر تاريخ".

**الحل (ثلاث نقاط):**

```python
# qanoonsa.py — مرّر السطور المستخرَجة كلها إلى skip
skip = {"English"}
if doc.gazette_ref:
    skip.add(doc.gazette_ref)
if doc.issued_date:
    skip.add(f"صدر في: {doc.issued_date}")
```

```python
# htmlmd.prose_to_markdown — طبّق نفس تنظيف بادئة الـ CMS على كل كتلة
text = _CMS_LABEL_PREFIX_RE.sub("", " ".join(el.get_text(" ", strip=True).split()))
```

```python
# lint_corpus.lint_file — حدّ أدنى للمتن بدل "غير فارغ"
_MIN_BODY_CHARS = 120
if len(content) < _MIN_BODY_CHARS:
    errors.append(f"متن أقصر من الحد الأدنى ({len(content)} حرفًا): يُحتمل فشل استخراج")
```

الحد الأدنى قابل للنقاش (بعض أوامر الإعفاء/التعيين قصيرة فعلًا)، فابدأ بجعله **تحذيرًا** لتقيس التوزيع، ثم ارفعه إلى خطأ صلب بعد تنظيف الدفعة الحالية. المهم أن ٧٪ من المُدوَّنة لا تمرّ بصمت.

#### 🟠 (ج) خطأ مستخدم شائع يخرج كـ traceback خام *(مُتحقَّق منه معمليًا)*

```
$ python -m scripts.main --from-file /nonexistent.txt
Traceback (most recent call last):
  ...
FileNotFoundError: [Errno 2] No such file or directory: '/nonexistent.txt'
```

نفس الشيء في `discover.py` عند `--out` بمسار غير قابل للكتابة. باقي الأداة تعالج أخطاءها بعناية ورسائلها بالعربية، فهذان الموضعان شاذّان. الحل: لُفّ قراءة `--from-file` وكتابة `--out` بـ `except OSError as exc: parser.error(f"تعذّر …: {exc}")`.

#### 🟡 (د) `|| true` يبتلع فشل الجلب كاملًا

في `.github/workflows/update-corpus.yml`:

```yaml
- name: جلب شرطي من المصدرين
  run: |
    python -m scripts.main --discover qanoonsa,nezams \
      --check-updates --resume --delay 2 --report || true
```

النية مفهومة (فشل رابط واحد يجب ألا يوقف الشهر كله)، لكن `|| true` يبتلع أيضًا **الفشل الكلي**: لو سقط الموقعان أو تغيّرت بنيتهما، تنتهي التشغيلة "بنجاح" بلا PR وبلا إشارة. الحل: احتفظ بالكود الخارج، واستخرج نسبة الفشل من `logs/summary.md`، وأفشِل الخطوة إذا تجاوز الفشل عتبة (مثلًا 20%) — فتصبح الأتمتة قادرة على الصراخ.

### 2.5 الأداء واستهلاك الموارد — **8/10**

الأداء مناسب للحجم، وفيه تحسين مقصود جيد: `_front_matter_head()` تقرأ كتلة الـ front matter بالسطر وتتوقف عند الفاصل الثاني بدل قراءة الملف كله — يوفّر قراءة 43 ميغابايت في كل تشغيلة.

لا يوجد ReDoS: اختبرت `_AMEND_LINE_RE` و`ARTICLE_LABEL_RE` على مدخلات مُعادية بطول 20,000 محرف، والزمن ≤ 3 ميلي ثانية.

**الملاحظة الوحيدة** — سلسلة الصيانة في `update-corpus.yml` تمرّ على المُدوَّنة كاملة أربع مرات متتالية:

```yaml
python -m scripts.canonicalize_urls laws   # مسح 1
python -m scripts.reclassify laws          # مسح 2
python -m scripts.audit_duplicates laws    # مسح 3 (يقرأ كل ملف كاملًا)
python -m scripts.build_index laws         # مسح 4
```

المسح الثالث يقرأ الـ 43 ميغابايت كاملة (`path.read_text()` لكل ملف). ليست مشكلة عند 2,700 ملف، لكنها تتضاعف خطّيًا. الحل عند الحاجة: `maintain.py` واحد يمرّ مرة ويطبّق المراحل الأربع على النص في الذاكرة.

**ملاحظة تشغيلية:** `timeout-minutes: 350` مع `--delay 2` يعني سقفًا نظريًا نحو 10,500 طلب — والسقف الصلب لـ GitHub Actions هو 360 دقيقة. مع نمو المُدوَّنة ستصطدم التشغيلة بالسقف وتُقتل في منتصفها بلا PR. أضِف `--limit` بدفعات شهرية، أو قسّم الـ workflow على مصدرين بوظيفتين متوازيتين.

### 2.6 الاختبارات — **7.5/10**

150 اختبارًا تعمل في أقل من ثانية، وتغطية مقيسة **88%** على `scripts/`:

```
scripts/main.py          236   29   88%
scripts/adapters/qanoonsa.py  88   4   95%
scripts/fetch.py          85    3   96%
scripts/discover.py       80   34   58%
scripts/reclassify.py     83   18   78%
TOTAL                   1345  158   88%
```

نوعية الاختبارات جيدة: fixtures من صفحات حقيقية، وحالات حافة مأخوذة من أعطال فعلية (المادة "٢٢٩ مكرر" الملتصقة بنص التعديل، بنود أولا/ثانيا، القرار بلا مواد).

**الثغرات:**

- `canonicalize_urls.py` و`smoke.py` **بلا أي اختبار** (لا يظهران في تقرير التغطية أصلًا لأن لا شيء يستوردهما). `canonicalize_urls` سكربت يعيد كتابة 2,737 ملفًا؛ سكربت بهذه الصلاحية بلا اختبار مخاطرة غير ضرورية.
- `discover.py` عند 58% — مسار `run()` والتعامل مع خرائط فرعية معطوبة غير مغطّى.
- **الفجوة الأهم:** لا يوجد اختبار واحد يؤكّد **ثوابت على مستوى المُدوَّنة** — وهي بالضبط الفئة التي كانت ستكشف الـ 191 وثيقة الجوفاء. أضِف `tests/test_corpus_invariants.py` يفحص عيّنة (أو الكل) ويؤكّد: كل ملف له front matter صالح YAML، `source` ضمن قيم معروفة، طول المتن فوق حد أدنى، لا ضجيج واجهة. هذا يحوّل `lint_corpus` من "سكربت يُشغَّل" إلى "عقد مُختبَر".

### 2.7 التوثيق — **9/10**

`README.md` من أفضل ما يُكتب لمشروع بهذا الحجم: تنويه صريح في الأعلى بأن المستودع غير رسمي مع روابط المصادر الرسمية، فهرس محتويات، شرح كل خيار CLI بمثال، شرح لسبب وجود كل سكربت صيانة، وشفافية نادرة عن القيود:

> «يبقى تحدٍّ بنيوي أعمق: أغلب ملفات مجلدات النوع … لا تحمل تصنيف مجال في بياناتها الوصفية»

الترخيص مفصول بوضوح ومكرّر في ثلاثة مواضع متسقة (`LICENSE` + `README` + `laws/README.md`): MIT للكود، والنصوص ملك عام. هذا التزام صحيح ومهم لمشروع يعيد نشر محتوى مواقع أخرى.

**الخصم:** `README` يقول «يضم `laws/` نحو **2840** ملف Markdown» بينما العدد الفعلي **2,737**. رقم يدوي يتقادم في كل دفعة. الحل: ولّده من `index.json` في نفس خطوة `build_index` التي تعمل في CI أصلًا، أو استبدله بشارة (badge).

### 2.8 اتفاقيات التسمية ونمط موحّد — **9/10**

`snake_case` في كل مكان، والخاص مسبوق بشرطة سفلية باتساق، والثوابت `UPPER_CASE`. `ruff` بإعدادات جادّة (`E,F,I,UP,B`) ويمرّ نظيفًا. `from __future__ import annotations` في كل ملف. التلميحات النوعية (type hints) كاملة تقريبًا على التواقيع العامة.

**الخصم الوحيد:** الاستيراد الخاص عبر الوحدات المذكور في 2.3.

### متوسط القسم الهندسي

| النقطة | الدرجة |
|---|---:|
| البنية المعمارية | 9 |
| القراءة والصيانة | 8 |
| DRY و Code Smells | 7 |
| معالجة الأخطاء والحالات الحدّية | 5 |
| الأداء | 8 |
| الاختبارات | 7.5 |
| التوثيق | 9 |
| التسمية والنمط | 9 |

**المتوسط: 7.8/10**

---

## 3. تجربة الاستخدام (30%)

**تعديل المعيار:** المشروع بلا واجهة رسومية، فبنود «الاستجابة» و«breakpoints» و«تباين الألوان» لا تنطبق. قيّمتُ بدلها ثلاث واجهات حقيقية للمستخدم: **(أ)** واجهة الطرفية (CLI)، **(ب)** تصفّح المُدوَّنة على GitHub — وهي الواجهة الأساسية فعليًا لأغلب المستخدمين، **(ج)** الاستهلاك الآلي عبر `index.json` والـ front matter.

### 3.1 اتساق المخرجات البصرية — **8/10**

قالب الـ Markdown موحّد فعلًا عبر 2,737 ملفًا: front matter ثم `# العنوان` ثم `## المادة …` (أو `## الباب` + `### المادة` داخل الأبواب) ثم اقتباس التعديلات. تحقّقت من صلاحية الـ front matter كـ YAML على **كل** ملفات المُدوَّنة:

```
YAML-invalid front matter: 0  |  no front matter: 1 (laws/README.md — مستثنى بحق)
```

صفر ملف معطوب. وحقل `note` يحمل التنويه بعدم الرسمية داخل كل ملف — قرار صحيح، لأن الملفات تُنسخ خارج سياق المستودع.

### 3.2 سهولة التصفّح والتنقّل — **5/10**

هذه أضعف نقطة في تجربة الاستخدام.

**المشكلة الأولى: مجلد واحد فيه 1,306 ملفًا.**

```
  1306  laws/قرار/
   237  laws/لائحة/
   194  laws/مرسوم ملكي/
   194  laws/غير-مصنف/
   120  laws/أمر ملكي/
    88  laws/أنظمة التجارة والاقتصاد والاستثمار/
```

عارض الملفات في GitHub **يقطع عرض المجلد عند 1,000 عنصر**، فـ 306 ملفًا في `laws/قرار/` غير قابلة للوصول بالتصفّح إطلاقًا — لا تظهر ولا يمكن النقر عليها؛ الوصول الوحيد إليها عبر البحث أو `git clone`. المجلد الأكبر يحوي **48% من المُدوَّنة كلها**.

**الحل:** قسّم `قرار` بمستوى ثانٍ — الأنسب هنا السنة الهجرية (متاحة في `issued_date`) لأن القرارات إجرائية ومؤرّخة بطبيعتها: `laws/قرار/١٤٤٣/…`. بديل أضعف: تقسيم بحرف أول. المهم ألا يتجاوز أي مجلد ~300 ملف.

**المشكلة الثانية: 194 ملفًا في `غير-مصنف/`** — 7% من المُدوَّنة بلا تصنيف، والاسم نفسه يخبر القارئ أن لا معنى لوجودها هنا. الأسماء داخله موحية بأن أغلبها قابل للتصنيف («تعديلات اللائحة التنفيذية لنظام ضريبة الدخل» → مال ورقابة).

**المشكلة الثالثة: لا `README.md` داخل أي مجلد تصنيف.** `laws/README.md` موجود وجيد، لكن من يفتح `laws/قرار/` يجد جدارًا من 1,306 اسمًا بلا أي دليل. الحل رخيص: وسّع `build_index.py` ليولّد `README.md` لكل مجلد فيه جدول (عنوان · نوع · تاريخ · رابط) — يُولَّد آليًا فلا يتقادم، ويجعل كل مجلد صفحة تصفّح بدل قائمة ملفات.

### 3.3 قابلية الاستهلاك الآلي — **5/10**

`index.json` فكرة صحيحة ومنفَّذة جيدًا (مرتّب بالمسار لتقليل ضجيج git، ويُتحقّق منه في CI). لكن **البيانات نفسها** غير قابلة للاستعلام:

**(أ) `status` حقل نصّ حرّ.** أهم سؤال يُطرح على مُدوَّنة قانونية — «هل هذا النظام ساري؟» — لا جواب آلي له:

```
('ساري', 426)
('غير ساري', 12)
('غير ساري تم إلغاءه بصدور؛ نظام البيئة؛ ', 5)
('غير ساري؛ ألغي بصدور نظام الزراعة الصادر بالمرسوم الملكي رقم (م/٦٤) بتاريخ ١٤٤٢/٨/١٠هـ', 4)
('ألغيت بموجب قرار مجلس الوزراء رقم (٣٤٩) بتاريخ 22 / 6 / 1443 هـ', 1)
('ملغي بصدور نظام الأسماء التجارية …', 1)
('إلغي هذا النظام بصدور …', 1)
```

خمس صيغ مختلفة لمعنى «ملغى»، وواحدة فيها خطأ إملائي («إلغي»)، وأخرى بفواصل منقوطة متسربة من دمج أسطر الجدول. وفوق ذلك **2,257 من 2,737 وثيقة (82%) بلا حقل `status` إطلاقًا**.

**الحل:** حقلان بدل واحد — `status: نافذ | ملغى | معدّل | غير معروف` (قيم مغلقة يتحقق منها `lint_corpus`)، و`status_note` نصًّا حرًّا لبيان جهة الإلغاء. الاشتقاق ممكن آليًا بقواعد نصية على القيم الحالية.

**(ب) الأرقام مختلطة بين عربية-هندية ولاتينية داخل نفس الحقل:**

| الحقل | أرقام عربية-هندية | أرقام لاتينية |
|---|---:|---:|
| `issued_date` | 1,625 | 1 |
| `publish_date` | 84 | 362 |
| `approval_date_hijri` | 70 | 277 |

أي مستهلك يريد الفرز الزمني عليه أن يتعامل مع الصيغتين. الحل: طبّع الأرقام إلى اللاتينية في الحقول الآلية (`scripts/arabic_numbers.py` فيه `_ARABIC_DIGITS` جاهزة)، وأضِف `issued_date_hijri` بصيغة `1443-10-04` قابلة للفرز مع إبقاء النص الأصلي في حقل منفصل.

**(ج) قيمة `source` خارج المخطط.** وثيقة واحدة أُضيفت يدويًا تحمل:

```json
{"source": "مرفق", "source_url": "attachment:نظام-العمل-ولائحته-التنفيذية-الإصدار-الثاني-١٤٤٣هـ"}
```

بينما `schema.py` يوثّق أن `source` هو `"qanoonsa"` أو `"nezams"` و`source_url` رابط. مدخل واحد يكسر أي مستهلك يفترض أن `source_url` قابل للفتح. الحل: إما إضافة `"مرفق"` رسميًا إلى قيم `source` المعروفة والتحقق منها في `lint_corpus`، أو نقل المرفق إلى حقل `attachment_ref` منفصل.

### 3.4 العربية و RTL — **7/10**

المُدوَّنة عربية بالكامل، وأسماء الملفات والمجلدات عربية — قرار صحيح للجمهور المستهدف ويعمل جيدًا على GitHub.

- ✅ حدّ 200 بايت لاسم الملف بعد UTF‑8 حساب صحيح (الحرف العربي بايتان)، وتحقّقت أن أطول مسار في المُدوَّنة 171 محرفًا وأن **صفر** مسار يتجاوز 200 محرف.
- ✅ `sanitize_filename` تزيل `/\:*?"<>|` وترفض الأسماء المكوَّنة من نقاط فقط.
- ⚠️ **البايدي (bidi) في الأسطر المختلطة:** GitHub يعرض Markdown بلا `dir="rtl"`، فأي سطر عربي ينتهي برقم لاتيني أو رابط يظهر بترتيب مقلوب بصريًا. مثال حقيقي من المُدوَّنة: `المرسوم الملكي رقم (م/83) وتاريخ 1446/03/19هـ`. الحل الجزئي: التزم بالأرقام العربية-الهندية **أو** اللاتينية داخل النص المتصل (وليس خلطهما)، وضع الروابط في أسطر مستقلة.
- ⚠️ `attachments`/`amendments` تُدمج بفاصلة منقوطة `؛` في سلسلة واحدة عند غياب القيمة، فتضيع بنية القائمة أحيانًا (ظاهر في قيم `status` أعلاه: `«… تم إلغاءه بصدور؛ نظام البيئة؛ »`).

### 3.5 إمكانية الوصول — **7.5/10**

للمُدوَّنة النصية، إمكانية الوصول = سلامة التسلسل الهرمي للعناوين وبنية الجداول. وهنا الكود واعٍ فعلًا:

```python
# formatter.py — تعليق فعلي في الكود
# المستوى لكل مادة على حدة: ### تحت باب/فصل (##)، و## لمادة بلا قسم.
# رفعُ كل المواد إلى ### لمجرد وجود قسم واحد في الوثيقة كان يقفز
# H1→H3 للمواد السابقة لأول باب، فيكسر التسلسل الهرمي للعناوين
```

ونفس الوعي في `htmlmd.prose_to_markdown` («لا `###` حتى لا يقفز التسلسل الهرمي مستوى فتتعطل قارئات الشاشة ومولّدات فهرس المحتويات»). الجداول تُحوَّل بصف ترويسة صريح وفاصل، وتُهرَّب `|` داخل الخلايا — تُقرأ صحيحًا بقارئ الشاشة.

**الخصم:** الوثائق الجوفاء (§2.4‑ب) هي أسوأ عطل وصول ممكن — القارئ يصل إلى عنوان بلا محتوى.

### 3.6 التغذية الراجعة — **7/10**

رسائل الطرفية عربية وواضحة، ونمط ثابت جيد: `[نوع] عدد وحدة ← مسار`. و`--report` يجمع كل شيء في `logs/summary.md` (نجاح/فشل/تخطٍّ + توزيع الأنواع + كل التحذيرات + أسباب الفشل) — حلٌّ صحيح بدل مطاردة `stderr`.

**النواقص:**
- لا مؤشّر تقدّم في تشغيلة تمتدّ ساعات على آلاف الروابط: لا `[350/2700]` ولا زمن متبقٍ تقديري. إضافة عدّاد في حلقة `run()` تكلفة سطر واحد.
- الـ traceback الخام على `--from-file` (§2.4‑ج) يكسر انطباع العناية الذي يبنيه باقي الإخراج.
- التحذيرات تُطبع سطرًا لكل واحد على `stderr` أثناء التشغيل — 324 تحذيرًا في المُدوَّنة الحالية تعني جدارًا من النص يغرق الرسائل المهمة. الأفضل: كتمها افتراضيًا مع عدّاد، والتفصيل في التقرير.

### متوسط قسم تجربة الاستخدام

| النقطة | الدرجة |
|---|---:|
| اتساق المخرجات | 8 |
| سهولة التصفّح | 5 |
| الاستهلاك الآلي | 5 |
| العربية و RTL | 7 |
| إمكانية الوصول | 7.5 |
| التغذية الراجعة | 7 |

**المتوسط: 6.6/10**

---

## 4. المراجعة الأمنية (25%)

**تعديل النطاق:** المشروع زاحف (crawler) بلا خادم ولا مصادقة ولا قاعدة بيانات ولا بيانات مستخدمين. البنود التالية **لا تنطبق** ولم تُحتسب: المصادقة والصلاحيات، الجلسات وJWT، تجزئة كلمات المرور، حقن SQL، `CORS`/`CSP`/`HSTS`. الدرجة محسوبة من المنطبق فقط. سطح الهجوم الحقيقي هنا ثلاثي: **مُدخل غير موثوق (HTML من موقعين خارجيين)** → **كتابة ملفات على القرص** → **التزام آلي في git عبر GitHub Actions**.

### 4.1 المفاتيح والأسرار — ✅ نظيف

مسح `scripts/` و`tests/` و`.github/` عن `api_key|secret|token|password` أعاد صفر نتيجة حقيقية. لا ملف `.env`، ولا أسرار في الإعدادات. الـ workflow يستخدم `${{ github.token }}` المؤقت فقط. `.gitignore` يستثني `logs/` — صحيح، لأن `logs/failed.txt` قد يحمل رسائل خطأ من الشبكة.

### 4.2 الحقن ومسار الملفات — 🟡 محصَّن بشكل جيد، مع فجوة صغيرة

لا `eval` ولا `exec` ولا `subprocess` ولا SQL في المستودع كله. الخطر الوحيد الواقعي هو **اجتياز المسار (path traversal)** لأن كلًّا من اسم الملف والمجلد مشتق من **عنوان وتصنيف يتحكم فيهما الموقع المصدر**. الدفاع موجود وصحيح:

```python
def sanitize_filename(name: str) -> str:
    name = re.sub(r'[/\\:*?"<>|]', " ", name)
    name = re.sub(r"\s+", " ", name).strip()
    encoded = name.encode("utf-8")[:_MAX_FILENAME_BYTES]
    name = encoded.decode("utf-8", errors="ignore").strip()
    if not name or set(name) <= {"."}:      # يمنع "." و".."
        return "بدون-عنوان"
    return name
```

يزيل الفواصل ويرفض `..` — عنوان خبيث مثل `../../.github/workflows/evil` يصير `.. .. .github workflows evil` داخل `laws/`. **جيد.**

**الفجوة 🟡:** الدفاع في دالة واحدة يجب استدعاؤها في كل موضع، ولا يوجد **تحقق نهائي** بأن المسار المكتوب فعلًا داخل `out_dir`. مسار جديد ينسى `sanitize_filename` = ثغرة صامتة. الحل — حارس واحد قبل كل كتابة:

```python
resolved = path.resolve()
if out_dir.resolve() not in resolved.parents:
    raise ValueError(f"مسار خارج مجلد المخرجات: {path}")
```

**نقطة إيجابية مهمة:** خرائط المواقع تُحلَّل بتعبير نمطي على `<loc>` لا بمحلّل XML، فلا سطح لـ XXE أو القنابل الأسّية (billion laughs). و`BeautifulSoup` بمحلّل HTML لا يعالج كيانات خارجية.

### 4.3 التحقق من المدخلات وسطح الشبكة — 🟠

**✅ قائمة مضيفين بيضاء مطبَّقة في الطبقتين.** الاكتشاف يرفض أي رابط خارج النطاقين:

```python
def _host_ok(url: str) -> bool:
    host = (urlparse(url).hostname or "").lower()
    return any(host == h or host.endswith("." + h) for h in _ALLOWED_HOSTS)
```

والمعالجة ترفض المصدر غير المعروف عبر `detect_source`. الاستخدام الصحيح لـ `urlparse(...).hostname` (لا `netloc`) يمنع خدع `user@evil.com`، والتحقق بـ `endswith("." + h)` يمنع `qanoonsa.com.evil.com`. **دفاع SSRF مكتوب بعناية.**

**🟠 الثغرة الحقيقية — التوجيه (redirect) يتجاوز القائمة البيضاء.** `requests` يتبع التحويلات افتراضيًا، والقائمة البيضاء تُفحص على **الرابط الأصلي فقط**. مضيف مصدر مخترَق يردّ `302` إلى `http://169.254.169.254/…` (بيانات وصفية سحابية) أو إلى شبكة داخلية، والطلب يُنفَّذ. الأثر في تشغيل محلي محدود، لكنه أوسع في CI. الحل في `fetch.Fetcher._request`:

```python
response = self.session.get(url, timeout=self.timeout, headers=headers, allow_redirects=False)
if response.is_redirect:
    target = response.headers.get("Location", "")
    if not _host_ok(urljoin(url, target)):
        raise FetchError(f"تحويل إلى مضيف غير مسموح: {target}")
```

**🟠 لا سقف لحجم الاستجابة.** `response.text` يحمّل الجسم كاملًا في الذاكرة بلا حدّ — صفحة (أو ردّ خبيث) بحجم غيغابايت تُسقط العملية. الحل: `stream=True` مع فحص `Content-Length` وقطع القراءة عند سقف (مثلًا 20 ميغابايت).

**🟡 `Retry-After` مُتجاهَل.** عند `429` يعيد الكود المحاولة بتراجع أسّي ثابت `2 ** (attempt + 1)` متجاهلًا ترويسة `Retry-After` التي يرسلها الخادم. ليست ثغرة، لكنها سوء أدب زحف قد يجلب حظرًا للـ IP. الحل: `time.sleep(max(backoff, int(response.headers.get("Retry-After", 0))))`.

### 4.4 البيانات الحساسة — ✅ لا ينطبق

المحتوى نصوص أنظمة منشورة رسميًا (ملك عام)، بلا أي بيانات شخصية ولا اعتماديات. لا حاجة لتشفير ولا لسياسة احتفاظ. كل الطلبات على HTTPS (`SITE_INDEX` وعيّنات `smoke.py` كلها `https://`) و`canonical_url` يجعل المخطط الافتراضي `https`.

### 4.5 الاعتماديات وسلسلة التوريد — 🟡

**✅ إيجابيات:** ثلاث اعتماديات إنتاج فقط (`requests`, `beautifulsoup4`, `lxml`) — كلها نشطة وواسعة الاستخدام. Dependabot مفعّل شهريًا لـ pip ولـ GitHub Actions معًا. الحدود الدنيا معقولة (`requests>=2.31` أي بعد CVE‑2023‑32681).

**🟡 الملاحظتان:**

1. **الـ Actions مثبّتة بوسم لا ببصمة (SHA).** `actions/checkout@v4` و`actions/setup-python@v5` و`actions/upload-artifact@v4` — الوسم قابل للنقل، فاختراق مستودع الـ action يصل مباشرة إلى وظيفة تحمل `contents: write`. الحل المعياري: `uses: actions/checkout@<full-sha>  # v4.2.2` (Dependabot يحدّث البصمات ويبقي التعليق).
2. **لا حدّ عُلوي ولا ملف قفل (lock).** `requests>=2.31` بلا سقف يعني أن أي إصدار رئيس كاسر يُثبَّت تلقائيًا في CI. أضِف `requirements.lock` بـ hashes لمسار CI، وأبقِ `pyproject` مرنًا للمستخدمين.

### 4.6 صلاحيات الـ Workflows — 🟡

```yaml
permissions:
  contents: write
  pull-requests: write
```

في `update-corpus.yml` — لازمة فعلًا (دفع فرع + فتح PR) ومحصورة في هذا الملف. والقرار التصميمي الأهم صحيح: التشغيلة تفتح **PR مسودة للمراجعة اليدوية** ولا تدفع إلى `main` مباشرة، فلا يدخل محتوى خارجي إلى الفرع الرئيس بلا إنسان.

**🟡 ملاحظتان للتحصين:**
1. `tests.yml` و`smoke.yml` **بلا كتلة `permissions`**، فترثان الافتراضي (قد يكون واسعًا حسب إعدادات المستودع). أضِف `permissions: contents: read` صراحةً لكليهما.
2. المحتوى المُلتزَم يأتي من مواقع خارجية. الحماية الحالية سلوكية (مراجعة يدوية) لا تقنية. `lint_corpus` بوّابة معقولة، لكنها — كما في §2.4‑ب — تمرّر الوثائق الجوفاء. تقوية الـ lint هي أيضًا تقوية أمنية.

### حصيلة القسم الأمني

لا ثغرة حرجة. البنود المنطبقة: أسرار ✅ · حقن/مسار 🟡 · تحقق مدخلات وSSRF 🟠 · اعتماديات 🟡 · صلاحيات CI 🟡. الوعي الأمني في الكود واضح (قائمة بيضاء في طبقتين، `hostname` لا `netloc`، رفض `..`، لا محلّل XML)، والنواقص كلها من فئة "تحصين" لا "ثغرة مفتوحة".

**الدرجة: 8.5/10**

---

## 5. الدرجة الإجمالية المرجّحة

| الناحية | الدرجة | الوزن | المساهمة |
|---|---:|---:|---:|
| تقنية وهندسية | 7.8/10 | 45% | 3.51 |
| تجربة الاستخدام | 6.6/10 | 30% | 1.98 |
| أمن سيبراني | 8.5/10 | 25% | 2.13 |
| **الإجمالي** | | | **7.6/10** |

**الحساب:** ‏(7.8 × 0.45) + (6.6 × 0.30) + (8.5 × 0.25) = 3.51 + 1.98 + 2.125 = **7.615 ≈ 7.6/10**

**قراءة الدرجة:** مشروع فوق المتوسط بوضوح، وهندسته أنضج من أغلب مشاريع الاستخراج المفتوحة — لكنه مشدود لأسفل بفجوة واحدة محدَّدة: **بوّابة جودة البيانات لا تحرس ما بُنيت لحراسته**. الأداة ممتازة، والمُدوَّنة التي تنتجها فيها 7% وثائق جوفاء تمرّ بصمت. أصلح ذلك وحده وتقفز الدرجة إلى نطاق 8.5.

---

## 6. خطة الإصلاح المرتّبة بالأولوية

| # | الأولوية | الناحية | المشكلة | الحل المقترح | التأثير بعد الحل |
|---|---|---|---|---|---|
| 1 | 🔴 حرج | هندسة | 191 وثيقة جوفاء + 12 وثيقة ضجيج خالص تمرّ في CI بصمت | `lint_corpus.lint_file`: استبدل `if not content` بحدّ أدنى `_MIN_BODY_CHARS = 120` (تحذيرًا أولًا ثم خطأً صلبًا)؛ و`qanoonsa.parse`: أضِف سطر `صدر في: {doc.issued_date}` إلى `skip`؛ و`htmlmd.prose_to_markdown`: طبّق `_CMS_LABEL_PREFIX_RE` على كل كتلة | لا وثيقة بلا نصّ قانوني تدخل المُدوَّنة؛ الـ CI يعكس جودة البيانات فعلًا |
| 2 | 🔴 حرج | هندسة | فقدان وثيقة كاملة إذا فشلت الكتابة بعد حذف القديم (مُعاد إنتاجه) | `main.process_html`: اكتب إلى `path.with_suffix(".md.tmp")` ثم `tmp.replace(path)` ثم احذف القديم. نفس النمط في `reclassify` وسكربتات الصيانة | استحالة فقدان وثيقة عند انقطاع/امتلاء قرص |
| 3 | 🔴 حرج | تجربة | `laws/قرار/` فيه 1,306 ملفًا — GitHub يقطع العرض عند 1,000، فـ 306 ملفًا غير قابلة للتصفّح | قسّم بمستوى ثانٍ حسب السنة الهجرية من `issued_date`: `laws/قرار/١٤٤٣/…`؛ وطبّق سقف ~300 ملف لكل مجلد في `lint_corpus` كتحذير | المُدوَّنة كلها قابلة للوصول بالتصفّح |
| 4 | 🟠 مهم | تجربة | `status` نصّ حرّ بخمس صيغ لـ«ملغى»، و82% من الوثائق بلا `status` | افصل: `status` بقيم مغلقة (`نافذ`/`ملغى`/`معدّل`/`غير معروف`) يتحقق منها `lint_corpus`، و`status_note` للنصّ الحرّ؛ واشتق القيم الحالية بقواعد نصية | «هل هذا النظام ساري؟» يصير استعلامًا آليًا — أهم سؤال في مُدوَّنة قانونية |
| 5 | 🟠 مهم | أمن | التحويل (redirect) يتجاوز قائمة المضيفين البيضاء | `fetch._request`: `allow_redirects=False` + فحص `Location` بـ `_host_ok` قبل المتابعة | إغلاق مسار SSRF عبر مصدر مخترَق |
| 6 | 🟠 مهم | أمن | لا سقف لحجم الاستجابة — استنزاف ذاكرة | `fetch._request`: `stream=True` + رفض `Content-Length` فوق 20 ميغابايت + قطع القراءة عند السقف | مناعة ضد صفحة/ردّ ضخم |
| 7 | 🟠 مهم | هندسة | `\|\| true` في `update-corpus.yml` يبتلع الفشل الكلي بلا إشارة | احتفظ بكود الخروج؛ استخرج نسبة الفشل من `logs/summary.md` وأفشِل الخطوة فوق عتبة 20% | انهيار المصدر يُكتشف في الشهر نفسه لا بعد أشهر |
| 8 | 🟠 مهم | هندسة | `canonicalize_urls.py` و`smoke.py` بلا اختبارات، ولا اختبار لثوابت المُدوَّنة | `tests/test_canonicalize_urls.py` + `tests/test_corpus_invariants.py` (front matter صالح، `source` ضمن قيم معروفة، حدّ أدنى للمتن، لا ضجيج) | تحويل `lint_corpus` من سكربت يُشغَّل إلى عقد مُختبَر |
| 9 | 🟠 مهم | تجربة | خطأ `--from-file` بمسار خاطئ يخرج traceback خامًا | لُفّ القراءة في `main.run` وكتابة `--out` في `discover.run` بـ `except OSError as exc: parser.error(...)` | إخراج متسق بالعربية في كل مسارات الخطأ |
| 10 | 🟡 تحسين | تجربة | خلط الأرقام العربية-الهندية واللاتينية داخل نفس حقول التاريخ | طبّع الأرقام في الحقول الآلية عبر `_ARABIC_DIGITS`، وأضِف `issued_date_hijri` بصيغة `1443-10-04` قابلة للفرز | فرز زمني وبحث بلا معالجة مسبقة عند المستهلك |
| 11 | 🟡 تحسين | هندسة | `_extract_list` و`_LIST_FIELD_RE` مكرّران حرفيًا في 3 ملفات | أضِف `read_list_field()` في `frontmatter.py` واحذف النسخ الثلاث | مصدر واحد لتحليل الـ front matter |
| 12 | 🟡 تحسين | أمن | Actions مثبّتة بوسم لا ببصمة SHA؛ `tests.yml`/`smoke.yml` بلا `permissions` | ثبّت بـ SHA كامل مع تعليق الإصدار؛ أضِف `permissions: contents: read` للـ workflowين | تقليص أثر اختراق action خارجي |
| 13 | 🟡 تحسين | أمن | لا حارس نهائي يؤكد أن المسار المكتوب داخل `out_dir` | قبل كل `write_text`: `if out_dir.resolve() not in path.resolve().parents: raise ValueError(...)` | دفاع في العمق لا يعتمد على تذكّر `sanitize_filename` |
| 14 | 🟡 تحسين | تجربة | 194 ملفًا في `غير-مصنف/`، ولا `README` داخل مجلدات التصنيف | وسّع `build_index.py` ليولّد `README.md` لكل مجلد بجدول (عنوان · نوع · تاريخ · رابط)؛ ووسّع `_CATEGORY_ALIASES` للحالات الواضحة | كل مجلد يصير صفحة تصفّح بدل جدار أسماء |
| 15 | 🟡 تحسين | تجربة | لا مؤشّر تقدّم في تشغيلة تمتدّ ساعات؛ 324 تحذيرًا تغرق `stderr` | أضِف `[{i}/{total}]` في حلقة `run()`؛ اكتم تفصيل التحذيرات افتراضيًا مع عدّاد وأبقِ التفصيل في `--report` | إخراج مقروء في التشغيلات الطويلة |
| 16 | 🟡 تحسين | هندسة | `run()` في `main.py` تجاوزت 130 سطرًا بثلاث مسؤوليات؛ و`QanoonsaAdapter.parse` بآلة حالة من 5 متغيّرات | استخرج `_build_parser()` / `_collect_urls()` / `_process_urls()`؛ واستخرج `_ArticleCollector` من الـ adapter | قابلية اختبار كل قطعة وحدها |
| 17 | 🟡 تحسين | هندسة | `timeout-minutes: 350` قرب سقف GitHub الصلب (360) وسينكسر مع نمو المُدوَّنة | أضِف `--limit` بدفعات، أو قسّم الـ workflow لوظيفتين متوازيتين (مصدر لكل وظيفة) | التحديث الشهري يبقى يكتمل مع النمو |
| 18 | 🟡 تحسين | هندسة | `README` يذكر «نحو 2840 ملفًا» والعدد الفعلي 2,737 | ولّد الرقم من `index.json` في خطوة `build_index` القائمة في CI | رقم لا يتقادم |
| 19 | 🟡 تحسين | هندسة | `set_field`/`set_list_field` تمرّران القيمة كسلسلة استبدال لـ `re.sub`، فتُفكّ فيها `\\` | مرّر دالة بدل نص: `field_re.sub(lambda _: line, block, count=1)` | كامن اليوم (صفر شرطة عكسية في المُدوَّنة) لكنه يكسر الهروب متى ظهرت |
| 20 | 🟡 تحسين | هندسة | `lint_corpus` يستورد `_BROKEN_TITLE_RE` و`_NOISE_PATTERNS` الخاصّين من `schema` | اجعلهما عامّين ما دامت المشاركة مقصودة | عقد وحدات صريح |

---

## ملحق — ما جرى التحقق منه معمليًا

| الفحص | النتيجة |
|---|---|
| `python -m pytest` | 150 نجحت · 0.97 ثانية |
| `ruff check scripts tests` | نظيف |
| تغطية الاختبارات (`coverage`) | 88% على `scripts/` (1,345 عبارة · 158 غير مغطّاة) |
| `lint_corpus laws --warnings` | 2,737 ملفًا · 0 خطأ صلب · 324 تحذيرًا · **رمز الخروج 0** |
| `build_index laws --check` | محدَّث · رمز الخروج 0 |
| صلاحية الـ front matter كـ YAML | 2,737/2,737 صالحة · 0 معطوبة |
| وثائق متنها سطر إصدار فقط | **191** |
| وثائق متنها ضجيج واجهة خالص | **12** |
| وثائق متنها < 500 حرف | 570 (20.8%) |
| عناوين مزدوجة | 0 |
| ملفات بلا `source_url` | 0 |
| أطول مسار / مسارات > 200 محرف | 171 محرفًا / 0 |
| ReDoS على `_AMEND_LINE_RE` و`ARTICLE_LABEL_RE` | لا يوجد (≤3 ملي ثانية على 20,000 محرف) |
| فقدان وثيقة عند فشل الكتابة | **مُعاد إنتاجه** — الملف القديم يُحذف والجديد لا يُكتب |
| مسح الأسرار في `scripts/`, `tests/`, `.github/` | 0 نتيجة |
